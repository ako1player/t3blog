export type SlugType = {
    title: string,
    desc: string,
    slug: string,
    img: string,
};

export type Post = {
    id:string,
    title: string,
    desc: string,
    cat: string,
    catSlug: string,
    slug: string,
    img: string
}